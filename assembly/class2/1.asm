;输出BX最高位，二进制1000000000000000以上或者十六进8000H以上，BX最高位为1，否者为0
CODE SEGMENT 
ASSUME CS:CODE
START:
MOV BX,08000H ;初始化BX，为了区分字符F和十六进制数，用0开头
SHL BX,1 ;左移，BX*2
JC ONE
MOV DL,'0' ;这里的0是字符的0.不是整数
JMP TWO

ONE:MOV DL,'1';跳转之后下面继续顺序执行
TWO:MOV AH,'2'
INT 21H

CODE ENDS
END START
